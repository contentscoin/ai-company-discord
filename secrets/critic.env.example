# Copy to secrets/critic.env — one file per profile, one bot token each.
# NEVER commit the real file (.gitignore covers secrets/*.env).
DISCORD_BOT_TOKEN=

# Whichever provider this soul uses
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# OPENROUTER_API_KEY=
